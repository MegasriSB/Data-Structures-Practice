#include<stdio.h>
#include<Stdlib.h>
int main(){
    int A;
    int B;
    int n,m,i,c;
    char str[20];
    str = "6A5B3"
    printf("Enter the input String: ");
    scanf("%s",str);
    printf("Enter number of A: ");
    scanf("%d",&n);
    printf("Enter number of B: ");
    scanf("%d",&m);
    for(i = 0; i < strlen(str); i++)
    {
        if(str[i] == A)
            A = '+';
        else if(str[i] == B)
            B = '-';
    }
    printf(6"%c)
}
